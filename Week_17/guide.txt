Quote Generator App — Vite + React + API Ninjas
1. Create the Project

npm create vite@latest quote-generator -- --template react
cd quote-generator
npm install
2. Add Your API Key
Create .env in the project root:


VITE_API_KEY=your_api_ninjas_key_here
Add to .gitignore:


.env
3. Project Structure

quote-generator/
├── .env
├── index.html
├── src/
│   ├── main.jsx
│   ├── App.jsx
│   ├── App.css
│   └── components/
│       ├── QuoteCard.jsx
│       ├── Controls.jsx
│       └── ErrorMessage.jsx
4. src/main.jsx

import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import "./App.css";
import App from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <StrictMode>
    <App />
  </StrictMode>
);
5. src/App.jsx

import { useState } from "react";
import Controls from "./components/Controls";
import QuoteCard from "./components/QuoteCard";
import ErrorMessage from "./components/ErrorMessage";

const API_KEY = import.meta.env.VITE_API_KEY;
const BASE_URL = "https://api.api-ninjas.com/v2/quotes";

export default function App() {
  const [quote, setQuote] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState("");

  async function fetchQuote({ category, author }) {
    const params = new URLSearchParams();
    if (category) params.set("categories", category);
    if (author) params.set("author", author);

    setLoading(true);
    setError("");
    setQuote(null);

    try {
      const res = await fetch(`${BASE_URL}?${params}`, {
        headers: { "X-Api-Key": API_KEY },
      });

      if (!res.ok) throw new Error(`API error: ${res.status} ${res.statusText}`);

      const data = await res.json();

      if (!data.length) {
        setError("No quotes found. Try a different category or author.");
        return;
      }

      setQuote(data[0]);
    } catch (err) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  }

  return (
    <div className="app">
      <h1>Quote Generator</h1>
      <Controls onFetch={fetchQuote} loading={loading} />
      {error && <ErrorMessage message={error} />}
      {quote && <QuoteCard quote={quote} />}
    </div>
  );
}
6. src/components/Controls.jsx

import { useState } from "react";

export default function Controls({ onFetch, loading }) {
  const [category, setCategory] = useState("");
  const [author, setAuthor] = useState("");

  function handleKeyDown(e) {
    if (e.key === "Enter") onFetch({ category, author });
  }

  return (
    <div className="controls">
      <input
        type="text"
        placeholder="Category (e.g. wisdom, success)"
        value={category}
        onChange={(e) => setCategory(e.target.value)}
        onKeyDown={handleKeyDown}
      />
      <input
        type="text"
        placeholder="Author (optional)"
        value={author}
        onChange={(e) => setAuthor(e.target.value)}
        onKeyDown={handleKeyDown}
      />
      <button onClick={() => onFetch({ category, author })} disabled={loading}>
        {loading ? "Loading..." : "Get Quote"}
      </button>
    </div>
  );
}
7. src/components/QuoteCard.jsx

export default function QuoteCard({ quote }) {
  const { quote: text, author, work, categories } = quote;

  return (
    <div className="quote-card">
      <blockquote>"{text}"</blockquote>
      <p className="author">— {author}</p>
      {work && <p className="work">From: {work}</p>}
      <div className="tags">
        {categories.map((cat) => (
          <span key={cat} className="tag">
            {cat}
          </span>
        ))}
      </div>
    </div>
  );
}
8. src/components/ErrorMessage.jsx

export default function ErrorMessage({ message }) {
  return <div className="error">{message}</div>;
}
9. src/App.css
Replace the default with:


* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: system-ui, sans-serif;
  background: #0f172a;
  color: #e2e8f0;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 2rem;
}

.app {
  max-width: 640px;
  width: 100%;
}

h1 {
  text-align: center;
  font-size: 2rem;
  margin-bottom: 2rem;
  color: #7dd3fc;
}

.controls {
  display: flex;
  gap: 0.5rem;
  flex-wrap: wrap;
  margin-bottom: 1.5rem;
}

input {
  flex: 1;
  padding: 0.6rem 0.9rem;
  border-radius: 8px;
  border: 1px solid #334155;
  background: #1e293b;
  color: #e2e8f0;
  font-size: 1rem;
}

input::placeholder {
  color: #64748b;
}

button {
  padding: 0.6rem 1.4rem;
  border-radius: 8px;
  border: none;
  background: #0ea5e9;
  color: white;
  font-size: 1rem;
  cursor: pointer;
  transition: background 0.2s;
}

button:hover:not(:disabled) {
  background: #38bdf8;
}

button:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.quote-card {
  background: #1e293b;
  border-left: 4px solid #0ea5e9;
  border-radius: 8px;
  padding: 1.5rem;
}

blockquote {
  font-size: 1.25rem;
  font-style: italic;
  line-height: 1.6;
  margin-bottom: 1rem;
}

.author {
  font-weight: bold;
  color: #7dd3fc;
  margin-bottom: 0.25rem;
}

.work {
  font-size: 0.875rem;
  color: #94a3b8;
  margin-bottom: 0.75rem;
}

.tags {
  display: flex;
  flex-wrap: wrap;
  gap: 0.4rem;
}

.tag {
  background: #0f172a;
  border: 1px solid #334155;
  border-radius: 999px;
  padding: 0.2rem 0.7rem;
  font-size: 0.75rem;
  color: #94a3b8;
}

.error {
  color: #f87171;
  background: #1e293b;
  padding: 1rem;
  border-radius: 8px;
  border-left: 4px solid #f87171;
}
10. Run It

npm run dev
Open http://localhost:5173.

11. Build for Production

npm run build
npm run preview
Key React concepts used here:

useState — manages quote, loading, and error state in App
Props down / callbacks up — Controls calls onFetch, App owns the data
Conditional rendering — {quote && <QuoteCard />}, {error && <ErrorMessage />}
Controlled inputs — value + onChange in Controls